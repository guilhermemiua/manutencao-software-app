import styled from 'styled-components/native';
import { Input as InputElements } from 'react-native-elements';

export const Input = styled(InputElements)`
  color: #fff;
  margin: 0;
  padding: 0;
`;
